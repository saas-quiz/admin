import { IQuestion } from "@/types";
import React from "react";

const QuizQuestions = ({ questions, editable }: { questions: IQuestion[]; editable?: boolean }) => {
  return <div></div>;
};

export default QuizQuestions;
